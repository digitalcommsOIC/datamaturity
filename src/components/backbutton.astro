<button class="text-white flex flex-row gap-2 items-center bg-white bg-opacity-25 py-2 px-4 rounded-full border-solid border-[1px] border-white hover:scale-105 transition-all active:scale-95" back-button>
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor" class="size-5">
        <path fill-rule="evenodd" d="M17 10a.75.75 0 0 1-.75.75H5.612l4.158 3.96a.75.75 0 1 1-1.04 1.08l-5.5-5.25a.75.75 0 0 1 0-1.08l5.5-5.25a.75.75 0 1 1 1.04 1.08L5.612 9.25H16.25A.75.75 0 0 1 17 10Z" clip-rule="evenodd" />
    </svg>      
    Go Back
</button>

<script>
    document.addEventListener('astro:page-load', () => {
        const buttons = document.querySelectorAll('button[back-button]');

        buttons.forEach((button) => {
            button.addEventListener('click', () => {
                history.back();
            });
        });
    });
</script>